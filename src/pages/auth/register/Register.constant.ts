import { TComponentSeoMeta } from '@/types/common';

export const kRegisterPageMeta: TComponentSeoMeta = {
  descriptionPage: 'Register page for Custom Dashboard',
  titlePage: 'Register || Custom',
};

export enum kRegisterFormField {
  EMAIL = 'email',
  NAME = 'name',
  PASSWORD = 'password',
  CONFIRM_PASSWORD = 'conf_password',
}
